---
'@davepi/ui-app-react': patch
---

Add solid `bg-popover` background to `SelectItem`, `CommandItem`, and `DropdownMenuItem` so option text is always readable at rest, not just on hover/selection.
